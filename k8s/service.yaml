apiVersion: v1
kind: Secret
metadata:
  name: qshield-secrets
type: Opaque
stringData:
  api-key: "qshield_secret_key_2026"
---
apiVersion: v1
kind: Service
metadata:
  name: q-shield-service
  labels:
    app: q-shield-ecdat
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: q-shield-ecdat
